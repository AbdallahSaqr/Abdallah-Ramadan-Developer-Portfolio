# Absolute origin of the deployment. Used for canonical URLs, OG tags,
# sitemap.xml and robots.txt. On Vercel this is inferred automatically
# from VERCEL_PROJECT_PRODUCTION_URL, so it is only needed for a custom domain.
NEXT_PUBLIC_SITE_URL=https://your-domain.com
